PORT=6767
MONGO_URI="mongodb+srv://<user>:<password>@<cluster-url>/parpas?retryWrites=true&w=majority"
# Optional: set this after the Python teammate delivers the intelligence service.
# PYTHON_PREDICTIVE_BASE=http://localhost:8000
# Local demo only. Set false in any shared or deployed environment.
DEV_AUTH_BYPASS=true
# Optional: use a real User _id to make the fallback identity meaningful.
DEV_AUTH_USER_ID=
DEV_AUTH_ACCOUNT_TYPE=Student
VERIFICATION_SECRET=replace-with-a-long-random-secret
